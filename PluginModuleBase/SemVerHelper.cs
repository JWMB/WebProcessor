namespace PluginModuleBase
{
    public class SemVerHelper
    {
        public static void AssertClientVersion(string? clientVersion, string? minVersion, string? maxVersion)
        {
            //if (trainingPlan.clientRequirements.Version.Min != null && vClient < trainingPlan.clientRequirements.Version.Min)
            //    throw new Exception($"This training requires at least version {trainingPlan.clientRequirements.Version.Min} of the app"); //TODO: translate
            //if (trainingPlan.clientRequirements.Version.Max != null && vClient > trainingPlan.clientRequirements.Version.Max)
            //    throw new Exception($"This training requires an older version ({trainingPlan.clientRequirements.Version.Min}) of the app"); //TODO: translate
        }

    }
}
